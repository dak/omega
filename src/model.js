import csp from './csp/csp';
import Stream from './stream';

export default class Model extends Stream {

    constructor () {
        super();
    }

    /*
    MODELS
    	should support COMPUTED PROPERTIES
    	metadata
            look into using symbols to prevent clashing/overwriting some meta
    	properties
    	look into swift to get ideas
    		lazy stored properties
        maybe use WeakMap for private/meta properties
    */

}
